<% if @task.errors.present? %>
  <ul>
    <% @task.errors.each do |error, message| %>
      <li> <%= "#{error.capitalize}  #{message}" %></li>
    <% end %>
  </ul>
<% end %>

<div>
  <%= form_for @task do |f| %>
    <%= f.label :title, t("tasks.title") %>
    <%= f.text_field :title %>
    <%= f.label :content, t("tasks.content") %>
    <%= f.text_area :content %>
    <%= f.submit %>
  <% end %>
</div>
